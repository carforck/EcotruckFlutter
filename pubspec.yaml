name: ecotruckflutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.16.0
  material_color_utilities: ^0.11.1

  # Mapa para Android/iOS
  google_maps_flutter: ^2.5.0

  # Mapa para Web
  flutter_map: ^6.0.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/fondo_ecotruck.png
    - assets/logo_ecotruck.png
    - assets/Perfil_Carlos.jpg
